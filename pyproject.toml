[project]
name = "sim-generator"
version = "0.1.0"
description = "SIM card parameter generator"
requires-python = ">=3.10"

[project.scripts]
simgen = "sim_generator.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"