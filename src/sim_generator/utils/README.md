# SIM Generator

Генератор параметров SIM-карт (ICCID, IMSI, Key и другие).
Позволяет генерировать заданное количество SIM и сохранять их в CSV (`db/user_db.csv`).

---

## 🔹 Требования

* Python 3.10+
* pip

---

## 🔹 Установка

1. **Клонируем репозиторий**

```bash
git clone https://github.com/R0man0r/sim_generator.git
cd sim_generator
```

2. **Создаём и активируем виртуальное окружение**

```bash
# Linux / macOS
python3 -m venv .venv
source .venv/bin/activate

# Windows (PowerShell)
.venv\Scripts\activate
```

3. **Устанавливаем проект в editable режиме**

```bash
pip install --upgrade pip
pip install -e .
```

---

## 🔹 Использование

### Через CLI

После установки доступна команда `simgen`:

```bash
simgen 5
```

Где `5` — количество SIM-карт для генерации. Результат добавляется в CSV-файл:

```
db/user_db.csv
```

### Через Python

Можно использовать напрямую в Python:

```python
from sim_generator.main import main

# Сгенерировать 10 SIM
main(["10"])
```

> ⚠ Важно: при запуске через Python аргументы передаются как список строк.
